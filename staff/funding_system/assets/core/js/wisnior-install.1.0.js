"Use strict"

var wisnior = {
  init(){
    // var jxr = $.post(config.api + 'core/get_start.php', function(resp){ if(resp == 'No database install'){ window.location = config.domain + 'installation/'} })
  }
}

wisnior.init()
